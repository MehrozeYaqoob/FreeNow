<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Highlight" parent="AppTheme">
        <item name="android:textSize">@dimen/font_size_s</item>
        <item name="android:fontFamily">@font/roboto_bold</item>
    </style>

    <style name="HighlightMedium" parent="AppTheme">
        <item name="android:textSize">@dimen/font_size_m</item>
        <item name="android:fontFamily">@font/roboto_bold</item>
    </style>

    <style name="HighlightLarge" parent="AppTheme">
        <item name="android:textSize">@dimen/font_size_l</item>
        <item name="android:fontFamily">@font/roboto_bold</item>
    </style>

    <style name="HighlightSm" parent="AppTheme">
        <item name="android:textSize">@dimen/font_size_xs</item>
        <item name="android:fontFamily">@font/roboto_bold</item>
    </style>

</resources>
